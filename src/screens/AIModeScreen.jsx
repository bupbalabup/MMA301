import AppContainer from '../components/AppContainer';
import AppText from '../components/AppText';

export default function AIModeScreen() {
  return (
    <AppContainer>
      <AppText variant="title">AI Mode</AppText>
    </AppContainer>
  );
}
